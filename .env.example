# 국립해양측위정보원(NMPNT) 해양기상 서비스키 — 값은 커밋 금지, 환경변수로만 주입.
# 기상 수집(NMPNT) 단계에서 사용합니다.
NMPNT_SERVICE_KEY=

# 프로젝트 설정은 configs/default.yaml 기본값을 쓰고, 필요하면 MAR_* 환경변수로 덮어씁니다.
# 예) 사고-기상 매칭 거리 임계(km)
# MAR_MATCH_MAX_KM=60

# (선택) 라이브 모드 배경지도 — VWorld 키. 키 없이 동작하는 정적 데모에는 불필요.
# NEXT_PUBLIC_VWORLD_KEY=
