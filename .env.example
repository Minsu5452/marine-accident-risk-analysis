# 국립해양측위정보원(NMPNT) 해양기상 서비스키 — 커밋하지 말고 환경변수로만 주입합니다.
# 기상 수집(NMPNT) 단계에서 사용합니다.
NMPNT_SERVICE_KEY=

# 프로젝트 설정은 configs/default.yaml 기본값을 쓰고, 필요하면 MAR_* 환경변수로 덮어씁니다.
# 예) 사고-기상 매칭 거리 임계(km)
# MAR_MATCH_MAX_KM=60
