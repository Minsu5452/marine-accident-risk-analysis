# 좌표 정제 — 한국 EEZ를 대략 감싸는 좌표 범위(bbox). 이 범위 밖 좌표는 이상치로 제거합니다.
eez_lat_min: 32.0
eez_lat_max: 39.0
eez_lon_min: 124.0
eez_lon_max: 132.0

# 격자 해상도(도). 세 해상도를 모두 산출하고 프론트에서 선택합니다.
grid_resolutions: [0.05, 0.1, 0.25]

# 사고-기상 최근접 매칭 임계값(거리 km·시간 분).
match_max_km: 60.0
match_time_window_min: 30
